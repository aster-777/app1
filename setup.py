from setuptools import setup, find_packages

setup(
    name='01_HOME',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        'streamlit_lottie',
        'PIL'
        'requests'
        'streamlit'
        # Add other dependencies here
    ],
)
